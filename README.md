 Small amount of self-contained CUDA code to test precision of texture fetches and ability for normalized texture coordinates to land in exact texel centers (like you'd get with integer texel loads).
